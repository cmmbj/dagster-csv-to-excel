[GIT Bash]

1. cd " " : se mettre dans le dossier voulu
2. git clone https:// : avoir un repo sur son pc
3. git pull origin [branch] : si le repo est déjà sur le pc
4. git add . : ajoute tous les fichiers
5. git commit -m " " : donne desc de tes changements
6. git push origin [branch] : publie/envoie tes modif

[VS Code]

assets.py : colonne vertébrale du projet. Il va chercher le fichier csv, utilise pandas pour lire le csv, transforme les données, sauveard en .xlsx

__init__.py : rassemble tous les assets, les jobs et ressources dans Definitions (objet). Importe ce que j'ai écrit dans assets.py pour que Dagster puis l'afficher.

pyproject.toml : liste les biblio ext nécessaires (dagster, dagster-webserver, pandas, openpyxl)

data/inputs et outputs : trouver csv et xlsx

[Dagster]



[PowerShell]

## Server
pip install -e : installe le projet et télécharge pandas/openpyxl

dagster dev : lance dagster